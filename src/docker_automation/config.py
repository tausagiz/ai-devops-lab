# Runtime-wide constants — change here to apply everywhere.
IMAGE_TAG = "myapp:latest"
BUILD_PATH = "."
MANAGED_LABEL_KEY = "ai-devops-lab.managed"
MANAGED_LABEL_VALUE = "true"
MANAGED_LABEL_FILTER = f"{MANAGED_LABEL_KEY}={MANAGED_LABEL_VALUE}"
