---
name: "Fix Validation"
description: "Diagnose and fix failures from /Validate Changes, then rerun impacted checks."
agent: "Fix Validation"
argument-hint: "Optional: failing command/log snippet and preferred scope"
---
Fix validation problems on this branch.

Use `AGENTS.md` rules. Prefer minimal, reversible fixes and rerun failed checks first.

If still blocked, include one concrete cheapest next action.
