---
name: "Rescope Branch"
description: "Rename the current branch to match coherent scope drift and optionally update remote tracking."
agent: "Rescope Branch"
argument-hint: "Required: new branch name (type/short-slug). Optional: update remote yes/no"
---
Rename this branch so its name reflects the current coherent scope.

Use branch naming and safety rules from `AGENTS.md`.

If remote updates are requested, keep operations safe and confirmation-based.
